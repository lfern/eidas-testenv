[package]
name = "ca"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Test PKI chain generator (Root CA, Sub-CA, TSA, OCSP, user certs) — not implemented yet"

[dependencies]
